@model IEnumerable<webteste0.Models.Maquina>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome_maquina)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome_maquina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.maquina_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.maquina_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.maquina_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
